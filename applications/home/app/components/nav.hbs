<nav class="bg-blue-600 text-white p-4 flex items-center justify-between">
   <div class="hidden md:flex gap-6">
    {{#link-to "home" class="hover:underline"}}Home{{/link-to}}
    {{#link-to "panel" class="hover:underline"}}Panel{{/link-to}}
    {{#link-to "contact" class="hover:underline"}}Weather{{/link-to}}
  </div>
  <button class="md:hidden" onclick={{this.toggleMenu}} type="button">
    ☰
  </button>
</nav>

{{#if this.menuOpen}}
  <div class="bg-blue-500 text-white flex flex-col p-4 gap-4 md:hidden">
    {{#link-to "home" class="hover:underline" onclick=this.closeMenu}}Home{{/link-to}}
    {{#link-to "panel" class="hover:underline" onclick=this.closeMenu}}Panel{{/link-to}}
    {{#link-to "contact" class="hover:underline" onclick=this.closeMenu}}Weather{{/link-to}}
  </div>
{{/if}}

<hr>
{{outlet}}
